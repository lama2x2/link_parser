я:
привет, мне нужно составить спеку для проекта:

### CLI-утилита: Markdown Link & Dead Code Checker
> Суть: Консольный скрипт на Python или Node.js, который сканирует указанную папку с проектом.
- Функционал:
  - Находит все `.md` файлы в папке либо в гитхаб репе. 
  - Извлекает все ссылки `[текст] (url)` и `file:///...`).
  - Проверяет локальные ссылки на существование файлов, а внешние HTTP-ссылки — на доступность (отправляет `HEAD` или `GET` запрос).
  - Выводит красивый отчёт в консоль (с цветами/таблицей) со списком сломанных ссылок и статус-кодами.
- На чем "спотыкается" ИИ без хорошей спеки:
  - Обработка относительных путей относительно текущего файла.
  - Ограничение по таймаутам HTTP-запросов и обработка ошибок сети `404`, `500`, `timeout`).

клод:
Спека готова. Несколько решений, которые я принял за вас — поменяйте, если не согласны:

**Стек — Python** (`markdown-it-py` + `httpx` + `rich` + `typer`). В §1.3 указан прямой аналог на Node, вся логика §4–§9 переносится один в один.

**Главное про «спотыкания»:**
- Извлечение ссылок — строго через AST, не регуляркой. Регулярка гарантированно вытащит ссылки из блоков кода, и это ошибка №1 в таких реализациях. В §5.1 таблица «что извлекать / что игнорировать», в §5.2 — как достать номер строки, когда inline-токены `markdown-it` не хранят позицию.
- Резолвинг путей — §6.2 с готовым кодом и явным правилом: база = `source_file.parent`, а ссылка на `/docs/a.md` считается от корня проекта, а не от корня ФС. Плюс §6.4 про регистрозависимую проверку — без неё `./ReadMe.md` пройдёт на macOS и упадёт на GitHub.
- HTTP — три раздельных таймаута (connect / read / общий дедлайн со всеми ретраями), ретраи только для сетевых ошибок, 429 и 5xx, `HEAD` → `GET` fallback на 403/405, семафор на хост, circuit breaker и пропуск приватных IP.

**§14 — чек-лист на 15 пунктов** для ревью: если отдаёте спеку ИИ-агенту, попросите его в конце пройтись по нему построчно. **§13.2** — 32 тест-кейса с конкретными ожиданиями, их можно скормить как acceptance criteria.

Если хотите, могу переписать под Node.js или добавить режим `--fix` (автоисправление опечаток в путях), который я вынес за скоуп.




я:
спека справится с этой папкой? (прикрепил скрин папки)

клод:
Вижу папку: кириллические имена, пробелы в именах (`Работа с Git.md`), `readme` в нижнем регистре, и это, судя по всему, macOS. Спека справится примерно на 80% — но есть четыре дыры, из-за которых на этой конкретной папке она даст ложные срабатывания. Давайте я их закрою.Что было сломано конкретно для этой папки и что я дописал:

**1. NFD vs NFC — самая коварная.** Вы на macOS, а APFS хранит имена файлов в декомпозированной форме: `й` физически лежит как `и` + U+0306. Строка `Локальный запуск` из markdown-ссылки будет в NFC. Визуально идентичны, побайтово — нет. Мой прежний §6.4 требовал сравнивать «побайтово», то есть выдал бы `file_not_found` практически на каждой ссылке с `й`, `ё`. Теперь §6.4.1: обе стороны через `unicodedata.normalize("NFC", ...)`, к ФС обращаться только исходной строкой, в отчёт печатать NFC.

**2. `Работа с Git.md` — пробелы.** По CommonMark `[текст](Работа с Git.md)` вообще не ссылка: destination обрывается на первом пробеле, токен не создаётся. Парсер бы её просто не увидел — молча, без строки в отчёте. Хуже ложного срабатывания. Добавил второй проход регуляркой и код `unencoded_space`.

**3. `readme` в нижнем регистре.** Ровно тот случай, ради которого писалась проверка регистра: `[x](./README.md)` пройдёт у вас на маке и упадёт на GitHub. Плюс поправил `lower()` → `casefold()`, иначе `РАБОТА С GIT.md` не сматчится.

**4. «Локальный запуск» и «Доступы».** Там почти наверняка `http://localhost:3000` и внутренние хосты — по умолчанию уйдут в `SKIPPED: private_host`, что правильно. Добавил подсказку про `--allow-private-hosts`, когда таких больше 30%.

**Бонус:** если папка ведётся в Obsidian (имена с пробегами и кириллицей — характерный признак), там будут `[[Работа с Git]]` вместо обычных ссылок. Добавил §5.5 с автовключением по наличию `.obsidian/`.

Один вопрос по делу: эта папка — гит-репозиторий или просто локальная директория? Если репозиторий и он приватный, ссылки вида `https://github.com/company/repo/blob/main/...` будут отдавать 404 без токена — нужно будет прописать `--github-token` или занести в `ignore-url`.